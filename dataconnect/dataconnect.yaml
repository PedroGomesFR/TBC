specVersion: "v1beta"
serviceId: "tbc"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tbc-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
